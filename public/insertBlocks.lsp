; To use VisualLisp or VB .NET on AutoCAD PC to put project block.csv
; from server into drawing for a quick start

; INSERT SINGLE LINE
; Loop all coordinate from file
;  Loop all blocks
;   Loop all attributes in block
;    If UUID attribute
;     found == true
;  If found != true
;   Insert block per coordinates


; UPDATE SINGLE LINE
; Loop all block attributes from file
;  Loop all blocks
;   Loop all attributes in block
;    If UUID attribute
;     Update attriubtes from file
